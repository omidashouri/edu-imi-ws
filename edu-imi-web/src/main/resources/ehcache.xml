<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <!-- Persistent cache directory -->
    <persistence directory="/run/media/o.ashouri/Archive/0_0/cache" />

    <!-- Default cache template -->
    <cache-template name="default">
<!--        <expiry>
            <ttl unit="seconds">0</ttl>
        </expiry>-->
        <listeners>
            <listener>
                <class>edu.imi.ir.eduimiws.servces.CacheEventLogger</class>
                <event-firing-mode>ASYNCHRONOUS</event-firing-mode>
                <event-ordering-mode>UNORDERED</event-ordering-mode>
                <events-to-fire-on>CREATED</events-to-fire-on>
                <events-to-fire-on>EXPIRED</events-to-fire-on>
                <events-to-fire-on>EVICTED</events-to-fire-on>
            </listener>
        </listeners>
        <resources>
            <heap>100</heap>
            <offheap unit="MB">10</offheap>
            <disk persistent="true" unit="MB">200</disk>
        </resources>
    </cache-template>

    <cache alias="edu.imi.ir.eduimiws.domain.edu.RegisterEntity" uses-template="default"/>
    <cache alias="edu.imi.ir.eduimiws.domain.edu.RegisterApiEntity" uses-template="default"/>
    <cache alias="register" uses-template="default"/>
    <cache alias="registerApi" uses-template="default"/>
    <cache alias="student" uses-template="default"/>
    <cache alias="period" uses-template="default"/>

<!--    <cache alias="squareCache">
        <key-type>java.lang.Long</key-type>
        <value-type>java.math.BigDecimal</value-type>
        <expiry>
            <ttl unit="seconds">30</ttl>
        </expiry>

        <listeners>
            <listener>
                <class>edu.imi.ir.eduimiws.servces.CacheEventLogger</class>
                <event-firing-mode>ASYNCHRONOUS</event-firing-mode>
                <event-ordering-mode>UNORDERED</event-ordering-mode>
                <events-to-fire-on>CREATED</events-to-fire-on>
                <events-to-fire-on>EXPIRED</events-to-fire-on>
            </listener>
        </listeners>

        <resources>
            <heap unit="entries">2</heap>
            <offheap unit="MB">10</offheap>
        </resources>
    </cache>-->
</config>